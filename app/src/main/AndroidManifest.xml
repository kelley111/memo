<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Memo"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DiaryCreateActivity">
            <!-- 可能还有其他intent-filter等配置 -->
        </activity>
        <activity android:name=".DiaryActivity"></activity>
        <activity android:name=".SentenceCollectionActivity"></activity>
        <activity android:name=".DiaryWriteActivity"></activity>
        <activity android:name="dairy_favorite_collection"></activity>
        <activity android:name=".TodoList"></activity>

        <activity android:name=".Todoeditactivity" />




    </application>

</manifest>